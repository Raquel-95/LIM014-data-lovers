
// order from a to z and z to a 
// export const order = (array, string1) => {
//     const arrayData = [...array];
//     const newArray = arrayData.map((obj) => obj.name)
//       .sort().map((string2) => arrayData.find((obj) => obj.name === string2));
//     if (string1 === 'A-Z') {
//       return newArray;
//     } if (string1 === 'Z-A') {
//       return newArray.reverse();
//     } 
//   };












// export const order = {
//     ascending: (allCharacters) => {
//        return allCharacters.sort(function(a,b) {
//            if( a.name > b.name ) {
//                return 1;
//            }
//            if(a.name < b.name) {
//                return -1;
//            }
//            return 0;
//        });
//     },

//     descending:(allCharacters) => {
//         return allCharacters.sort(function(a,b) {
//             if(a.name > b.name) {
//                 return -1;
//             }
//             if(a.name < b.name) {
//                 return -1;
//             }
//             return 0;
//         });
//     },
    
// };

